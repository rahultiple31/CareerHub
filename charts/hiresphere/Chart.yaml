apiVersion: v2
name: hiresphere
description: Helm chart for hiresphere on K3s with React, Node API, PostgreSQL, Redis, OpenSearch, Nginx Ingress, cert-manager, and S3 storage
type: application
version: 0.4.0
appVersion: "2.2.0"
kubeVersion: ">=1.27.0-0"
